/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    esmExternals: false
  },
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig
    unoptimized: true
  },
}

module.exports = nextConfig
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
